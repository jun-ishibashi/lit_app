# テスト用。encrypted_password は Devise の digest（password123）
one:
  name: Fixture Provider One
  email: provider1@test.example.com
  encrypted_password: <%= Devise::Encryptor.digest(Provider, 'password123') %>
  service_type_id: 2
  introduction: intro

two:
  name: Fixture Provider Two
  email: provider2@test.example.com
  encrypted_password: <%= Devise::Encryptor.digest(Provider, 'password123') %>
  service_type_id: 2
  introduction: intro
